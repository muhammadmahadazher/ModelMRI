{
  "name": "modelmri-frontend",
  "private": true,
  "version": "0.6.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "build:demo": "tsc --noEmit -p tsconfig.json && vite build --mode demo",
    "build:viewer": "tsc --noEmit -p tsconfig.json && vite build --mode viewer",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource/archivo-black": "^5.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "typescript": "~5.6.3",
    "vite": "^6.0.7"
  }
}
