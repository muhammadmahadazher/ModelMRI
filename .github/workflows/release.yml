name: Release

# Tag vX.Y.Z -> build the frontend into the wheel, verify it, attach both
# artifacts to the GitHub release. (PyPI publish stays a manual `uv publish`
# with the owner's token.)
on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      # No dependency caching in this job, on purpose: it builds the
      # artifacts people install, and a poisoned cache would be a
      # supply-chain path straight into the wheel. It runs once per tag, so
      # the cache would save nothing worth that risk.
      #
      # Turning caching off EXPLICITLY, not by omission.
      #
      # Under setup-node v4 the only way in was the `cache:` input, so leaving
      # it out was the off switch. v7 added `package-manager-cache`, which
      # defaults to caching whenever package.json carries `packageManager` or
      # `devEngines.packageManager`. This repo has neither today, so the
      # upgrade changes nothing — but that makes the guarantee depend on a
      # field nobody is watching, in the one job whose cache would land inside
      # the artifacts people install. Say no out loud.
      - uses: actions/setup-node@v7  # zizmor: ignore[cache-poisoning]
        with:
          node-version: 22
          package-manager-cache: false
      - uses: astral-sh/setup-uv@v9
        with:
          enable-cache: false

      - name: Build frontend into the package
        run: |
          cd frontend
          npm ci
          npm run build
          # The .mri viewer ships inside the wheel too: `modelmri open` serves
          # it from the standard library, and without it that command can only
          # apologise. Neither bundle is committed, so if this line goes the
          # wheel goes out broken and nothing else notices.
          npm run build:viewer

      - name: Build wheel + sdist
        run: uv build

      - name: Verify both bundles are inside the wheel
        run: |
          python - <<'EOF'
          import glob, zipfile
          whl = glob.glob("dist/*.whl")[0]
          names = zipfile.ZipFile(whl).namelist()
          def has(prefix, ext):
              return any(prefix in n and n.endswith(ext) for n in names)
          assert has("static/app/", "index.html"), "app missing from wheel"
          assert has("static/app/assets/", ".js"), "app js missing"
          assert has("static/viewer/", "index.html"), "viewer missing from wheel"
          assert has("static/viewer/assets/", ".js"), "viewer js missing"
          print("wheel OK:", whl)
          EOF

      - name: Attach artifacts to the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" --verify-tag \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes dist/* \
            || gh release upload "${GITHUB_REF_NAME}" dist/* --clobber
